# SECURITY: Redirect all HTTP traffic to HTTPS
<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    # Redirect all requests to HTTPS
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</VirtualHost>

# HTTPS VirtualHost with TLS
<VirtualHost *:443>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html/public

    # SSL Configuration
    SSLEngine on
    SSLCertificateFile /etc/apache2/ssl/server.crt
    SSLCertificateKeyFile /etc/apache2/ssl/server.key

    # SECURITY: Strong TLS settings
    SSLProtocol all -SSLv3 -TLSv1 -TLSv1.1
    # SECURITY: Mozilla "Modern" cipher suite — forward secrecy (ECDHE) + authenticated
    # encryption (GCM) only. Excludes non-FS ciphers (AES-SHA), uncommon ciphers (CAMELLIA).
    SSLCipherSuite ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305
    SSLHonorCipherOrder on

    # SECURITY: HSTS header (tells browsers to always use HTTPS)
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

    # SECURITY: Only serve files from the public directory
    # Config, includes, and templates are NOT accessible from the web
    <Directory /var/www/html/public>
        Options -Indexes -FollowSymLinks
        AllowOverride None
        Require all granted

        # Prevent directory listing
        Options -Indexes

        # SECURITY: Disable server signature
        ServerSignature Off
    </Directory>

    # SECURITY: Block access to sensitive directories
    <Directory /var/www/html/config>
        Require all denied
    </Directory>

    <Directory /var/www/html/includes>
        Require all denied
    </Directory>

    <Directory /var/www/html/templates>
        Require all denied
    </Directory>

    # SECURITY: Prevent PHP execution in uploads directory
    <Directory /var/www/html/public/uploads>
        # Deny execution of PHP files in uploads
        <FilesMatch "\.ph(p[3457]?|t|tml)$">
            Require all denied
        </FilesMatch>

        # Force all files to be served as their actual MIME type
        # Prevents browsers from executing uploaded files
        <IfModule mod_headers.c>
            Header set X-Content-Type-Options "nosniff"
            Header set Content-Disposition "inline"
        </IfModule>

        # Only allow image files
        <FilesMatch "\.(?i)(jpe?g|png|gif|webp)$">
            Require all granted
        </FilesMatch>
    </Directory>

    # SECURITY: Block access to hidden files (e.g., .git, .env)
    <FilesMatch "^\.">
        Require all denied
    </FilesMatch>

    # SECURITY: Block access to SQL files
    <FilesMatch "\.sql$">
        Require all denied
    </FilesMatch>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>

# SECURITY: Disable server tokens (hide Apache version)
ServerTokens Prod
ServerSignature Off

# SECURITY: Remove X-Powered-By header (defense-in-depth, complements expose_php=Off)
Header always unset X-Powered-By
